{
  "name": "pozospharma-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute pozospharma-db --file=../schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
